import React from 'react';
import { ArrowRight, CheckCircle2, Eye, FileSearch, LockKeyhole, MapPin, ShieldCheck } from 'lucide-react';
import { Link, Outlet, useLocation } from 'react-router-dom';

const publicLinks = [
  { to: '/', label: 'Home' },
  { to: '/how-it-works', label: 'How it works' },
  { to: '/auth?next=/app/report', label: 'Report an issue' },
  { to: '/auth?next=/app/citizen', label: 'Track my report' },
  { to: '/about', label: 'About' },
];

export function PublicLayout() {
  const location = useLocation();
  return <div className="min-h-screen bg-[#020617] text-slate-100"><header className="sticky top-0 z-40 border-b border-slate-800/80 bg-[#020617]/95 backdrop-blur-xl"><div className="mx-auto flex min-h-16 max-w-7xl items-center justify-between gap-4 px-4 sm:px-6 lg:px-8"><Link to="/" className="flex shrink-0 items-center gap-2.5 rounded-xl focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-400"><span className="grid h-9 w-9 place-items-center rounded-xl bg-gradient-to-tr from-blue-600 to-cyan-400 text-sm font-bold text-white">CL</span><span className="font-heading text-lg font-bold text-white">CivicLens <span className="text-cyan-400">AI</span></span></Link><nav aria-label="Public navigation" className="hidden items-center gap-1 sm:flex">{publicLinks.map((link) => <Link key={link.to} to={link.to} className={`rounded-lg px-3 py-2 text-sm transition hover:bg-slate-800 hover:text-white ${location.pathname === link.to ? 'text-cyan-300' : 'text-slate-300'}`}>{link.label}</Link>)}<Link to="/auth" className="ml-2 rounded-lg border border-cyan-500/40 px-3 py-2 text-sm font-semibold text-cyan-300 transition hover:bg-cyan-500/10">Sign in</Link></nav><Link to="/auth" className="rounded-lg bg-cyan-500 px-3 py-2 text-sm font-semibold text-slate-950 sm:hidden">Sign in</Link></div></header><Outlet /></div>;
}

export function PublicHome() {
  return <main><section className="border-b border-slate-800/80 bg-[radial-gradient(circle_at_top_right,rgba(14,165,233,0.16),transparent_42%)]"><div className="mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8 lg:py-28"><div className="max-w-3xl"><p className="text-sm font-semibold uppercase tracking-[0.18em] text-cyan-300">Civic issue reporting and response</p><h1 className="mt-5 font-heading text-4xl font-bold tracking-tight text-white sm:text-6xl">Make your neighbourhood easier to care for.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-slate-300">CivicLens helps residents report public issues, review AI-assisted analysis, and follow each report through the response process.</p><div className="mt-8 flex flex-wrap gap-3"><Link to="/auth?next=/app/report" className="inline-flex items-center gap-2 rounded-lg bg-cyan-500 px-5 py-3 text-sm font-semibold text-slate-950 transition hover:bg-cyan-400">Report an issue <ArrowRight className="h-4 w-4" /></Link><Link to="/auth?next=/app/citizen" className="inline-flex items-center gap-2 rounded-lg border border-slate-700 px-5 py-3 text-sm font-semibold text-slate-200 transition hover:border-cyan-400 hover:text-white">Track my report</Link></div></div></div></section><section className="mx-auto grid max-w-7xl gap-4 px-4 py-16 sm:px-6 md:grid-cols-3 lg:px-8">{[{ icon: Eye, title: 'Review with AI assistance', text: 'Photo analysis can help identify the issue and estimate severity. You review the result before submitting.' }, { icon: MapPin, title: 'Route the right response', text: 'Your report includes the location and context needed for the responsible service team to act.' }, { icon: FileSearch, title: 'Follow the outcome', text: 'Sign in to see your submitted reports, status changes, and resolution notes.' }].map(({ icon: Icon, title, text }) => <article key={title} className="rounded-2xl border border-slate-800 bg-slate-900/50 p-6"><Icon className="h-6 w-6 text-cyan-400" /><h2 className="mt-5 font-heading text-lg font-semibold text-white">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{text}</p></article>)}</section><section className="border-y border-slate-800/80 bg-slate-950/50"><div className="mx-auto grid max-w-7xl gap-8 px-4 py-14 sm:px-6 md:grid-cols-3 lg:px-8">{[{ icon: ShieldCheck, title: 'Built for public trust', text: 'Your reports are associated with your authenticated account and access is enforced through Firebase security rules.' }, { icon: LockKeyhole, title: 'Privacy-aware by design', text: 'CivicLens uses only the information needed to process and follow up on your report.' }, { icon: CheckCircle2, title: 'Responsible AI', text: 'AI output is advisory and may contain errors. Review the analysis before you submit.' }].map(({ icon: Icon, title, text }) => <div key={title} className="flex gap-4"><Icon className="mt-1 h-5 w-5 shrink-0 text-emerald-400" /><div><h2 className="font-semibold text-white">{title}</h2><p className="mt-1 text-sm leading-6 text-slate-400">{text}</p></div></div>)}</div></section></main>;
}

export function PublicInfoPage({ title, intro, children }: { title: string; intro: string; children: React.ReactNode }) {
  return <main className="mx-auto max-w-4xl px-4 py-16 sm:px-6 lg:px-8"><p className="text-sm font-semibold uppercase tracking-[0.18em] text-cyan-300">CivicLens AI</p><h1 className="mt-4 font-heading text-4xl font-bold text-white">{title}</h1><p className="mt-4 max-w-2xl text-lg leading-8 text-slate-300">{intro}</p><div className="mt-12 space-y-8 text-sm leading-7 text-slate-400">{children}</div></main>;
}

export function HowItWorksPage() {
  return <PublicInfoPage title="How it works" intro="A clear path from a resident's report to a documented civic response."><section><h2 className="text-xl font-semibold text-white">1. Submit a report</h2><p className="mt-2">Sign in, add a description and location, and upload a photo when one is available.</p></section><section><h2 className="text-xl font-semibold text-white">2. Review AI assistance</h2><p className="mt-2">CivicLens can analyze the image to suggest a category, severity, and responsible department. The analysis is advisory and you remain in control of submission.</p></section><section><h2 className="text-xl font-semibold text-white">3. Follow progress</h2><p className="mt-2">Your authenticated account gives you access to your reports and the status updates recorded by authorized staff.</p></section></PublicInfoPage>;
}

export function AboutPage() {
  return <PublicInfoPage title="About CivicLens" intro="CivicLens is a civic technology platform for clearer reporting and accountable issue response."><section><h2 className="text-xl font-semibold text-white">For residents and response teams</h2><p className="mt-2">Residents get a straightforward way to report public issues. Authorized teams get a focused workspace for reviewing, assigning, and resolving those reports.</p></section><section><h2 className="text-xl font-semibold text-white">Privacy and access</h2><p className="mt-2">Account access, incident records, and uploaded evidence are handled through Firebase Authentication, Firestore, and Storage rules. Access depends on the authenticated user's role and scope.</p></section></PublicInfoPage>;
}
